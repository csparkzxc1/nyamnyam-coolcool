// Global Jest setup. Runs once after the test environment is ready,
// before each test file executes.
// Reserved for future global mocks (AsyncStorage, expo-notifications, etc.)
// when components depend on them.

export {};
