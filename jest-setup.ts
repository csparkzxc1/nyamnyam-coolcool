// Global Jest setup. Runs once after the test environment is ready,
// before each test file executes.
//
// Note: @testing-library/react-native v12+ ships with built-in matchers
// (toHaveTextContent, toBeOnTheScreen, etc.), so no extend-expect import
// is needed here. Reserved for future global mocks (AsyncStorage,
// expo-notifications, etc.) when components start depending on them.
export {};
